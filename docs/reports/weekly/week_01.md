# **Weekly Report — Week 1**

**Period: **Tuesday Jan 20 – Monday Jan 26, 2026

**Student: **Hadi Al Shmaissani

**Project: **Prediction of Driver Aggressiveness Level via Driving Behavior (Project 3)

**Supervisor: **Prof. Naseem Daher

## **Objectives**

Re-orient after the winter break, re-read the Milestone-1 progress report cover-to-cover, and convert Prof. Daher's mid-January feedback into a concrete second-term roadmap. The headline feedback was that the Q-learner from the midterm is essentially memorizing rather than learning, since the supposedly impressive accuracy curve is computed against ground-truth labels generated by the same closed-form AI equation that produces the score. Fixing that critique conceptually drives most of the term's work.

## **Work completed**

Audited the repository to clarify what was actually committed versus what the midterm report implied existed. The supposed PyTorch DynamicWeightAgent did not yet exist in the codebase — only the numpy DriverQLearner. This clarified that the term's PyTorch agent is net-new code rather than a refactor.

Drafted a one-page design memo for the new agent: inputs are a kinematic tuple (n_speed, n_accel, n_prox, n_wave) plus an environmental context vector (one-hot road type, normalized traffic density, normalized friction coefficient, weather flag); outputs are four AI weights (w_speed, w_accel, w_prox, w_wave); loss combines a supervised KL term against hand-picked target weights with an RL term that maximizes 1.0 − AI on non-crash episodes.

Cleaned up the repository structure. Created a `legacy/` folder for the Phase-1 scripts (aggressiveness_index.py, gym_simulation.py, main.py, simple_env.py) and reorganized the active codebase into `model/`, `agent/`, `env/`, `notebooks/`, and `scripts/`. Pinned dependencies via pyproject.toml: Python 3.11, PyTorch 2.2, gymnasium 1.0.0, highway-env 1.10.1.

Lit triage. Re-read Kiran et al. (2022), Wang et al. (2017), Zhu et al. (2021) with sharper attention. Added Vaswani et al. (2017) and the Shalev-Shwartz RSS paper to the queue.

## **Key results**

Roadmap with four primary tracks ranked by importance: (1) PyTorch DynamicWeightAgent; (2) gating / attention layer for soft regime transitions; (3) SUMO transition for population-scale simulation; (4) ROS2 wrapper. CARLA placed in the stretch column due to the 30 GB install footprint and dedicated GPU requirement.

## **Issues encountered**

Migrated the legacy data_collector.py to the new highway-env API: configure(dict) replaces config.update(dict) in highway-env 1.10.1. Verified the legacy pipeline still produces driving_behaviors_dataset.csv end-to-end.

## **Plan for next week**

Stand up the PyTorch DynamicWeightAgent skeleton: a 2-layer MLP that takes an 8-dim input (4 telemetry + 4 context) and outputs a 4-dim weight vector via softmax. Get a single forward and backward pass running end-to-end on cached telemetry from the Phase-1 CSV. No environment integration yet — just shape-checking and gradient flow.

VIPP 201A Weekly Reports  —  Hadi Al Shmaissani  —  Page  of
